# Nirmaan Calc

Construction calculator — native Android app (Kotlin, no extra libraries).

**Download:** [NirmaanCalc.apk](../../releases/latest/download/NirmaanCalc.apk)

Every push to `main` builds a new APK automatically (GitHub Actions) and publishes it under Releases.
